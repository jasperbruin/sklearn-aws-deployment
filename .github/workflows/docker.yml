# .github/workflows/docker.yml
name: Docker Build

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  docker:
    uses: jasperbruin/autodevops/.github/aws-ml-deploymen/docker-build.yml@main
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write